# HANDOFF — serenity-trainer training-parity campaign (2026-06-08)

Resume point for: making `/home/alex/serenity-trainer` models **train with parity** vs the OneTrainer oracle, one model at a time. Full running log: `docs/TRAINING_PARITY_CAMPAIGN.md` (read it). This file is the clean entry point.

## 0. THE ORACLE (read first — this is the whole game)
- **OneTrainer = THE oracle.** `/home/alex/OneTrainer` (stock Nerogar, `git@Nerogar/OneTrainer`), venv `/home/alex/OneTrainer/venv/bin/python` (torch 2.9.1+cu128, CUDA). It implements ALL these models (Chroma/Ernie/Flux2=Klein/Qwen/SDXL/SD3/Z-Image/...).
- **`/home/alex/Serenity` (the fork that made the frozen dumps) is DELETED.** So are `/home/alex/onetrainer-mojo` and `/home/alex/OneTrainer-anima-ref`. The dump scripts `scripts/*_dump_train_ref.py` point at the deleted Serenity and can't be re-run as-is.
- **Anima is NOT in stock OneTrainer** — it's PR #1487 (`dxqb/OneTrainer` branch `anima`), CLONED to `/home/alex/OneTrainer-anima` with its OWN venv `/home/alex/OneTrainer-anima/venv` (torch 2.9.1+cu128, transformers 5.9, diffusers@b003a47). Run Anima oracle with that venv + `PYTHONPATH=/home/alex/OneTrainer-anima`.
- **OneTrainer oracle harness pattern** (proven): load OneTrainer's own model + `LoRAModuleWrapper`, feed the FROZEN `trace.*` inputs from `parity/<m>_train_ref_step000.safetensors` + `adapter_before.*`, run the transformer call + loss EXACTLY as `Base<M>Setup.predict` (but do NOT use predict's internal noise/timestep gen — feed frozen), backward, dump `.grad`. Validates dump-vs-OneTrainer AND gives reference grads. Scripts: `scripts/ot_{klein,ernie,chroma,sdxl,anima_real}_oracle.py`. Grad refs: `parity/ot_{klein,ernie,chroma,sdxl,anima_real}_grads.safetensors`.

## 1. BUILD / GATE COMMANDS
- Mojo build+run (from /home/alex/mojodiffusion cwd): `cd /home/alex/mojodiffusion && rm -f serenitymojo.mojopkg && timeout 480 prlimit --as=40000000000 pixi run mojo build -I . -I /home/alex/serenity-trainer/src -Xlinker -lm -Xlinker -lcuda <smoke-abs> -o /tmp/x && /tmp/x`. `-lcuda` REQUIRED for offload paths. RUN with NO prlimit (offload needs full host VA). ONE build at a time (shared mojopkg).
- serenitymojo torch gates: `pixi run mojo build -I . -Xlinker -lm -Xlinker -lcuda serenitymojo/models/<m>/parity/lora_stack_parity.mojo -o /tmp/g && /tmp/g`.
- OneTrainer oracle: `PYTORCH_ALLOC_CONF=expandable_segments:True /home/alex/OneTrainer/venv/bin/python scripts/ot_<m>_oracle.py` (Anima: the clone venv + PYTHONPATH).
- GPU: RTX 3090 Ti 24GB. Large models need bf16 + gradient checkpointing + empty_cache.

## 2. STATUS PER MODEL
| Model | Trains-w-parity? | Forward | Backward | OneTrainer oracle |
|---|---|---|---|---|
| **Chroma** | ✅ DONE | dump cos 0.99996; OT-vs-dump EXACT (0.999993) | flux torch 72/75 + d_A=0; ot_chroma_grads available (resolves prior "not gateable") | exact |
| **Ernie** | ✅ DONE | Mojo 0.9977 (bf16 floor); OT-vs-dump 0.9972 (fork diffusers skew) | torch 49/49 cos 0.9999; 2 real bugs FIXED (text mask + rope text_len; tanh→exact-erf GELU) | 0.997 skew |
| **Anima** | ✅ DONE | Mojo 0.99942/0.99864 == OT 0.99910 (both vs fork-dump residual) | torch 64/64; **real training bug FIXED** (driver used simplified single-axis rope → real 3D `anima_dit.mojo:319 build_anima_3d_rope`) | PR#1487, 0.999 (fork dump-gen residual, NOT diffusers/Mojo) |
| **SDXL** | ✅ Stage1 (attn-LoRA) | rect 0.99999 (FIXED square-only→[H,W]) | runs (FIXED bf16/F32 crash via mixed_base in ops/norm.mojo+norm_backward.mojo); ST-attn torch 44/44; OT-vs-dump EXACT | exact; ot_sdxl_grads = full-UNet ref (TE confirmed OUT) |
| **Klein** | ✅ TRAINS (overfit-verified); d_B≠OT is benign bf16 numerics | cos 0.99995, d_A=0 OK | backward MATH correct (sdpa/rope/rms torch-verified ✓). d_B "1.5-2× off OT" = bf16 op-order between two valid bf16 impls (Mojo no further from F32 than OT; VALID per-block F32 ref) amplified by tiny ill-conditioned txt grad — NOT a bug. **OVERFIT CONVERGENCE VERIFIED: 30-step single-batch train, MSE 0.5877→0.0416 monotonic → bf16 LoRA grads ARE a correct descent direction.** smoke/klein_overfit_convergence.mojo | exact (OT==dump) |
| **Qwen** | ✅ block LoRA math VERIFIED; full-model HW-blocked | — | `serenitymojo/models/qwenimage/parity/qwenimage_block_lora_parity` PASS: all 24 adapters (img+txt × q/k/v/out/ffu/ffd) cos≥0.9997 + d_img/d_txt 0.9998 (has REAL txt joint-attn, not placeholder) | OT oracle BLOCKED: Qwen-Image transformer is **39GB** (~20B) > 24GB GPU, fwd+bwd doesn't fit; offload breaks training backward (confirmed via Klein this session). Per-block torch gate IS the authoritative math proof |
| **SD 3.5** | ✅ TRAINS (block overfit-verified) + block/LoRA math VERIFIED (oracle fidelity confirmed vs real diffusers) | joint-block fwd cos~1.0 (`sd35_block_parity`, MEASURED live 2026-06-09) | `serenitymojo/models/sd35/parity/sd35_block_parity` PASS (re-run live): joint-block fwd+bwd (ctx+x: wqkv/wproj/fc1/fc2/norms/mod) all cos≈1.0 (F32 ~1e-8) + LoRA x-qkv d_A/d_B cos 0.99998/0.99999; `sd35_lora_all_slots_smoke` wires all 8 slots finite+nonzero | **ORACLE FIDELITY INDEPENDENTLY CONFIRMED**: `sd35_block_vs_diffusers.py` drives the REAL diffusers `JointTransformerBlock(qk_norm=rms_norm, use_dual_attention=False, context_pre_only=False)` — the class OneTrainer `StableDiffusion3Model` uses (SD3Transformer2DModel) — at real dims (D1536/H24/Dh64/MLP6144) and matches the oracle math at **cos=1.0 (F64, max_abs ~1e-7)** on fwd(both streams) + d_hidden/d_encoder/d_temb + **all 32 weight grads** (incl joint-attn add_q/k/v, qk-rms norm_added, AdaLN ×2, ff ×2). LoRA delta == OT `LoRAModule.forward` (same already-verified formula). **OVERFIT-CONVERGENCE PROVEN (block-level)**: `sd35_overfit_convergence.mojo` — joint block + all 8 LoRA slots, fwd→MSE(both streams)→bwd→clip→AdamW ×70 on one batch; MSE **0.0450 → 6.0e-4 (74.6×)**, 1 jitter step, no divergence → bf16 LoRA grads ARE a correct descent direction. REMAINING (full-model 1024² — MEASURED 2026-06-09, two real blockers): **(1) dual-attention block — DONE at block level (2026-06-09).** sd3.5-medium blocks 0–12 (13/24) and sd3.5-large blocks 0–12 (13/38) are DUAL-attention (`x_block.attn2` + `SD35AdaLayerNormZeroX`, adds `hidden += gate_msa2·attn2(norm_x2)`). Ported: `sd35_block.mojo::sd35_dual_joint_block_forward/backward` (+ `Attn2Weights/Grads`, `DualXSaved`, `DualBlockForward/Grads`). Math locked vs REAL diffusers `JointTransformerBlock(use_dual_attention=True)` in `sd35_dual_block_vs_diffusers.py` (cos=1.0 F64, fwd+inputs+40 weight grads). Mojo gate `sd35_dual_block_parity.mojo` PASS: fwd both streams + d_x/d_ctx + all ctx/x/attn2 weight grads + msa2 mod grads cos~1.0; LoRA d_A/d_B on x-qkv + attn2-qkv cos 0.99999. STILL TODO: wire dual blocks 0–12 into `sd35_stack_lora` (select dual vs standard by block index) — the stack currently calls only the non-dual forward. **Two loader unknowns RESOLVED this session (measured): (a)** dual x adaLN chunk-9 order is `[shift_msa,scale_msa,gate_msa,shift_mlp,scale_mlp,gate_mlp,shift_msa2,scale_msa2,gate_msa2]` — verified via diffusers single_file_utils.py: `x_block.adaLN_modulation.1.weight`→`norm1.linear.weight` is an IDENTITY map (no row reorder), and `SD35AdaLayerNormZeroX.forward` chunks in that order; first 6 == existing `_chunk6`, last 3 = msa2 triple. **(b)** the offload plan (`build_sd35_block_plan`) streams the WHOLE `joint_blocks.{i}` prefix, so `x_block.attn2.*` keys already stream for dual blocks — NO plan change needed. **Composition VERIFIED (2026-06-09):** `sd35_dual_chain_{oracle.py,parity.mojo}` chains dual block A → standard block B and the Mojo gate PASSES — chained fwd cos~1.0, input grads d_ctx0/d_x0 threaded back through both blocks cos~1.0, weight grads reach EACH block (A.attn2.wqkv + B.x.wqkv) cos~1.0. So the dual↔standard dispatch the offload loop must do is proven correct; the offload wiring is now mechanical + run-verify, not uncertain.
Remaining wiring (one focused chunk): `_attn2_weights_from_block` + a 9-chunk `_block_dual_modvecs` (chunk order verified above); make `BlockSaved` hold dual-or-standard fwd; dispatch `sd35_dual_joint_block_{forward,backward}` for bi<13 in `sd35_stack_lora_{forward,backward}_offload`; add attn2 LoRA slots to `build_sd35_lora_set` for dual blocks. **DUAL DISPATCH WIRED + MEMORY DE-RISKED (2026-06-09, measured).** `sd35_stack_lora_{forward,backward}_offload` now dispatch the dual block for `bi<num_dual` (helpers `_compute_dual_modvecs` 9-chunk, `_chunk9_msa2`, `_attn2_weights_from_block`; `BlockSaved` holds dual-or-standard; zero-init LoRA grads so dual blocks' unused slots are AdamW no-ops). `serenitymojo/training/sd35_medium_overfit_smoke.mojo` (real medium ckpt, 1024²/N_IMG=4096/S=4250) RUNS the dual dispatch on REAL weights through blocks 0-22 at **~4GB GPU → medium 1024² FITS with huge headroom** (answers "medium will fit" — measured). **Also fixed 2 real offload bugs found here:** `turbo_loader._extract_block_prefix` (cut after 2nd dot → collapsed `model.diffusion_model.joint_blocks.N.` into one super-block → "no tensors for prefix"; now cuts after first all-numeric segment) and `sd35 _x_embed` (fed rank-4 conv weight to linear; now reshapes [D,16,2,2]→[D,64]). **3-VARIANT STACK COMPLETE + RUNS E2E (2026-06-09, measured).** Implemented `sd35_context_preonly_{forward,backward}` (final block 23: x standard, ctx qkv-only via AdaLayerNormContinuous, no ctx output, backward propagates d_context) and wired all three variants (dual 0-12 / standard 13-22 / ctxpre 23) into `sd35_stack_lora_{forward,backward}_offload`. **All 3 block variants now independently gate-verified** (`sd35_block_parity`, `sd35_dual_block_parity`, `sd35_ctxpre_parity` all PASS cos~1.0) + compose (`sd35_dual_chain_parity`) + **run full-depth on the real medium ckpt at 1024² (~5.8GB GPU, fits with huge headroom)**. **OPEN: the full medium run produces NaN from step 0** (fwd NaN; LoRA-B=0 so not LoRA). The ctxpre gate PASS proves it is NOT a block-math bug → HYPOTHESIS: the smoke's synthetic sin-fill inputs are pathological through 24 real blocks (the real trainer uses real cached latents/text). Also ~124s/step (slow host-`List[Float32]` carrier path). **MEDIUM 1024² TRAINS — VERIFIED (2026-06-09).** Wired the REAL OneTrainer cache into the smoke (latent→(l-VAE_SHIFT)*VAE_SCALE→channel-major `_pack_latents` [4096,64]; real text [154,4096] + pooled [2048]; flow-match). MEASURED on the real medium ckpt at 1024² (dual 0-12 + standard 13-22 + ctxpre 23, all offload): MSE **DECREASES MONOTONICALLY 35.529→31.575 over 6 steps** (finite grads, no NaN, ~5.8GB). Confirms the prior NaN was the synthetic inputs, not a block bug. So medium 1024² LoRA training descends end-to-end. **pos_embed WIRED (2026-06-09).** SD3 PatchEmbed positional embedding loaded into `SD35StackBase` + center-cropped (top/left=(MAX-H)//2, MAX=384) and added to image tokens after `x_embedder` (per diffusers `PatchEmbed.cropped_pos_embed`). MEASURED with pos_embed: medium 1024² MSE still decreases monotonically **34.671→30.384** (6 steps, finite, ~5.8GB). So the faithful forward (dual+standard+ctxpre + pos_embed) trains end-to-end. **FULL-MODEL FORWARD PARITY vs diffusers: PASS cos 0.99999987 (2026-06-09).** `sd35_fullfwd_{oracle.py,parity.mojo}` drove the REAL diffusers `SD3Transformer2DModel` (single_file medium) and the Mojo offload stack forward on IDENTICAL real cached inputs → cos **0.99999987**, max_abs 0.0137 (F32-tight). The WHOLE Mojo forward (t/y conditioning + pos_embed crop + dual 0-12 + standard 13-22 + ctxpre 23 + final layer) is faithful to the real model. **BUG FOUND by this check (first run was cos 0.11): patch feature orders DIFFER — x_embedder conv is (c,ph,pw) but diffusers proj_out/unpatchify (and thus Mojo `final_layer.linear`/`fwd.out`) is (ph,pw,c) (`reshape(...,p,p,C); einsum nhwpqc->nchpwq`). The trainer/smoke build the MSE target as `noise-latent_packed` in (c,ph,pw) [`_pack_latents`] → MIS-ORDERED vs `fwd.out` (ph,pw,c). So the earlier "medium trains" descent was on a SCRAMBLED target (HYPOTHESIS, strongly inferred — not re-trained to confirm). FIX: pack the velocity TARGET in (ph,pw,c) (output order), keep the noisy INPUT in (c,ph,pw); applies to both `train_sd35_real.mojo` and the smoke.** **TARGET-ORDER FIX APPLIED+VERIFIED (smoke).** Built flow-match in pixel space, pack noisy INPUT (c,ph,pw) [`_pack_latents`] + velocity TARGET (ph,pw,c) [`_pack_latents_out`, matches `fwd.out`]. MEASURED: medium 1024² MSE now descends **45.582→34.695** (6 steps, 1.31× — steeper than the mis-ordered 1.14×, grad_norm ~2-4). So medium 1024² trains on the CORRECT, parity-verified objective. NET: sd3.5-medium 1024²/N_TXT=154 LoRA training is **forward-parity-verified (cos 0.99999987) + trains on the correct target**, end-to-end on real weights+data, fits ~5.8GB. **REAL MEDIUM TRAINER WORKS (2026-06-09):** `serenitymojo/training/train_sd35_medium.mojo` — usable trainer on the verified path (iterates the OneTrainer cache via listdir, fresh flow-match noise/step, correct pack orders, fwd→MSE→bwd→clip→AdamW, saves PEFT LoRA). MEASURED (6 steps/3 samples, ~127s/step, ~5.8GB): iterates all samples, finite losses, LoRA-B 0→2036 (98/192 wired adapters learning = std 80 + ctxpre 5 + dual x-qkv 13), per-sample loss decreases across epochs, saved a VALID PEFT lora (384 tensors, correct `transformer.joint_blocks.N.*.lora_{A,B}.weight` keys/shapes, 24 blocks). So sd3.5-medium 1024²/N_TXT=154 LoRA training is END-TO-END WORKING + forward-parity-verified. **CACHE-DATA CHECK DONE (measured):** the cache latents are inconsistent — sample 10 std 3.62 (valid SD3 raw VAE latent), sample 11 std 36.6, sample 1 std **321.2 (min/max -37376/72704 — not a valid latent)**; text/pooled consistent. Per-sample MSE tracked it exactly (242347≈(321·1.53)²) → a cache-PREP data problem, not a trainer bug (HYPOTHESIS: samples 1/11 wrong scale/normalization). Added a latent-std guard (skip if std>20) — MEASURED: trainer now skips 1+11 with a warning and trains the valid sample 10 (MSE 45.575→45.526), LoRA-B 0→812, saves 192 PEFT pairs. Remaining: (a) regenerate the cache consistently (or confirm sample-10-scale is the intended norm) for a real multi-sample run; (b) perf ~127s/step (host-`List[Float32]` carrier path); (c) attn2 LoRA slots for dual blocks (x-qkv only now); (d) `train_sd35_real.mojo` (large) still needs the same fixes + gradient checkpointing. Medium is the working path. **(2) large 1024² OOMs** (measured CUDA_ERROR_OUT_OF_MEMORY after base load): the stack saves ALL blocks' activations (`saved.blocks`, no gradient-checkpointing) so 16GB base + S=4250 activations > 24GB; config asks `gradient_checkpointing:ON` but the Mojo stack does not implement it. Medium (~5GB base, 24 blocks) likely fits once dual-attn is added. **Path: (a) port dual-attention joint block (attn2 + SD35AdaLayerNormZeroX) for blocks 0–12; (b) add activation recompute/gradient-checkpointing for large; then full-stack 1024²/N_TXT=154 overfit.** |
| **Z-Image** | ✅ TRAINS (overfit-verified) + block/LoRA math VERIFIED (oracle fidelity confirmed vs real diffusers) | base block fwd cos 0.99999 (`zimage/parity/block_parity`) | `serenitymojo/models/zimage/parity/zimage_block_lora_parity` PASS: NextDiT main block + all 7 LoRA slots (to_q/k/v/out + SwiGLU w1/w3/w2) — fwd 0.99999, d_x 0.99999, norm/adaLN-mod grads 0.99999, **14 LoRA d_A/d_B all cos≥0.99997** (rank 8, scale 2.0). Frozen base proj grads empty by design (LoRA freezes base). Interleaved half-width 3-axis rope (32\|48\|48, θ256) | **ORACLE FIDELITY INDEPENDENTLY CONFIRMED**: `zimage_block_vs_diffusers.py` drives the REAL diffusers `ZImageTransformerBlock(modulation=True)` (the exact class OneTrainer `ZImageModel` uses) at real dims (D3840/H30/Dh128/F10240) and matches the oracle's hand math at **cos=1.0 (F64, max_abs ~1e-7)** on fwd + d_x + all 15 weight grads (incl qk-norm/rope path + adaLN). LoRA delta == OT `LoRAModule.forward` L328-329 (`orig+ld*alpha/rank`, A=[r,in] B=[out,r]) — code-identical. So Mojo→oracle→diffusers/OT chain is closed for block math. **OVERFIT-CONVERGENCE PROVEN**: `zimage/parity/zimage_overfit_convergence.mojo` runs the REAL multi-block stack (1 noise-refiner + 1 context-refiner + 2 main + final, S=10) through fwd→MSE→bwd→clip→AdamW for 50 steps on one fixed batch; MSE driven **0.0444 → 4.7e-5 (945× reduction)**, no divergence (max@step≥3 = 0.007 ≪ start) → bf16 LoRA grads ARE a correct descent direction. (Strict per-step monotonicity not required — sub-1e-4 jitter is the bf16 noise floor.) Z-Image now at the Klein "TRAINS" bar. |

NOT reachable (no oracle/not built): Flux.1, SD1.5, HiDream, HunyuanVideo, PixArt, Sana, Wuerstchen.

## 3. OPEN WORK (priority order, per user "fix Klein then qwen and sd 3.5")
### A. Klein d_B bug — DECISIVE TEST RUN (2026-06-08): REAL ADJOINT BUG in joint-attn backward, in/before sdpa_backward
**RESULT (measured, byte-identical step001 inputs, B=0 so forward = pure base, fwd cos 0.99995):**
Dumped per-projection d_y at double-block-0 image stream — OT `transformer_blocks[0].attn.to_q/k/v` MODULE-OUTPUT grads (`scripts/ot_klein_block0_dy_oracle.py` → `parity/ot_klein_block0_dy.safetensors`, fwd-hook+tensor register_hook fires through grad-ckpt) vs Mojo `d_q/d_k/d_v_pre_flat` (`KLEIN_DY_DUMP` env in `_stream_pre_backward_lora_resident_scratch` → `parity/mojo_klein_block0_dy.safetensors`):

| proj | cos(Mojo,OT) | OT L2 | Mojo L2 | ratio M/OT |
|---|---|---|---|---|
| d_q | 0.485 | 6.48e-5 | 1.07e-4 | 1.65× |
| d_k | 0.413 | 4.68e-5 | 8.16e-5 | 1.74× |
| d_v | 0.482 | 2.25e-4 | 4.04e-4 | 1.79× |

(to_q d_B reproduces: OT/dump 0.000276 vs Mojo 0.000473 = 1.72×.) **INTERPRETATION:** d_y wrong in BOTH magnitude (~1.7×) AND direction (cos~0.45) ⇒ NOT dtype/bf16 (would be cos~0.999), NOT the LoRA d_B math (it only consumes d_y), NOT rope_backward/rms_norm_backward. **d_v is wrong and its Mojo path is ONLY `sdpa_backward → slice → reshape`** (no rope, no rms) ⇒ the error is IN or BEFORE `sdpa_backward`: either (a) sdpa_backward math (`serenitymojo/ops/attention_backward.mojo`), (b) its `d_att_joint` input (from `_stream_post_backward_lora_resident_scratch` — note to_out d_B also off 1.44×), or (c) saved fwd `q_rope/k_rope/v_joint`. cos~0.45 (not a pure scale) favors a directional/adjoint error (transpose/joint-split/double-count) — consistent with suspect commit `5839693 "Split Klein qkv row outputs"`.
**LOCALIZATION (2 rounds RUN 2026-06-08, all measured vs OT on step001, B=0):** bisected the backward chain by dumping Mojo grads at progressive depths + OT-hook references (all keys in `parity/ot_klein_block0_{datt,dio,bnd}.safetensors`; Mojo `.datt/.dio/.bnd` siblings of `mojo_klein_block0_dy.safetensors`):
| probe point | img cos | txt cos | note |
|---|---|---|---|
| single↔double BOUNDARY (after singles bwd, before doubles) | **0.993** | **0.322** | ← img CORRECT, txt already corrupted |
| d_io/d_to ENTERING block-0 bwd (after doubles 1..N-1) | 0.775 | 0.270 | img degraded by txt contamination via joint attn |
| d_att (input to sdpa_backward) block 0 | 0.485 | 0.260 | |
| d_q/d_k/d_v_pre (to_q/k/v module-output d_y) block 0 | 0.41-0.49 | — | (the d_B-driving d_y) |

**CONCLUSION:** the error is BORN in the **single-block backward** (`models/klein/single_block.mojo single_block_backward` + the stack's recompute/saved path in `klein_stack_lora.mojo:1019-1044`). Text grad is SEEDED ZERO at the single-stack output (`klein_stack_lora.mojo:1007` — text output discarded in fwd) and generated wholly by the single joint-attention backward; image grad enters correct and stays ~correct through singles. Image cos is 0.993 (NOT 1.0) ⇒ a SMALL ABSOLUTE error exists in image too — catastrophic only for the ~10× smaller text grad (OT L2 img 2.79e-3 vs txt 2.49e-4). In the doubles the corrupted (small, cos-wrong) txt grad then bleeds into img via joint sdpa, dropping img 0.993→0.485. So likely ONE shared bug in the single-block joint-attention backward (suspects: `sdpa_backward`, `rope_backward`, or a SAVED/RECOMPUTE activation lifetime issue feeding them). NOTE: the named suspect commit `5839693` touches single_block.mojo qkv handling but its changes are FORWARD-path (verified correct via loss) — so it is NOT confirmed; look at the backward consumer + the recompute path (`single_block_lora_recompute_saved_device_resident_scratch`, SGL_SAVE_TAIL=9: most blocks RECOMPUTE saved q/k/v — a recompute≠forward discrepancy would corrupt bwd while loss stays exact, matching commit 5839693's OWN documented footgun "scratch-backed saved q/k/v rewound → silently corrupts grads, forward loss unchanged").
**ACTIVATION HYPOTHESIS REFUTED (RUN 2026-06-08):** Mojo self-check — fed the last single block's SAVED input x back through the RECOMPUTE path, compared every field (q_pre/k_pre/v/q_rope/norm/out_in): **byte-identical, max|Δ|=0.000e+00** (`klein_stack_lora.mojo:1021` block-23 dump → `mojo_klein_block0_dy.safetensors.sblk`). So saved AND recompute activations are faithful — NOT the scratch-lifetime footgun. ⇒ **the bug is in the Klein single-block BACKWARD OP MATH**, not the activations it reads. (Klein uses its OWN copy-in `models/klein/{single,double}_block.mojo` backward, NOT the shared Chroma/Flux path — so a Klein-specific adjoint bug doesn't break the passing Chroma/Ernie/Anima gates even though they share the underlying `sdpa_backward`/`rope_backward` ops; suspect Klein's CALL of them: qkv split/join, rope table, or scale.)
**OP-ISOLATION RUN (2026-06-08): sdpa_backward/rope_backward are CORRECT; error is a SMOOTH per-single-block TEXT-grad degradation (conditioning/precision-flavored).**
- Single block 23 (runs FIRST in bwd, clean input from head): sdpa INPUT d_att img cos 0.9999, sdpa OUTPUT sb.d_v cos ~1.0 (txt 0.9995). `sdpa_backward`+`rope_backward` produce CORRECT grads. (txt d_att ~0 at blk23 — text grad not yet accumulated.)
- Per-block running d_x (`klein_stack_lora.mojo .dxall` dump vs OT per-single-block input-grad hooks `parity/ot_klein_block0_sdx.safetensors`): IMG cos stays ≥0.993 all 24 blocks. TXT cos degrades SMOOTHLY 23→0: 0.997, 0.99(blk10), 0.962(7), 0.877(5), 0.692(3), 0.415(1), 0.318(0). NO cliff block. txt L2 MATCHES OT until ~blk3 then inflates (blk0 Mojo 3.41e-4 vs OT 2.50e-4 = 1.37×).
- Saved-vs-recompute activations byte-identical (refuted lifetime bug). dtype ruled out as SOLE cause by cos magnitude earlier, BUT the smooth-degrade-of-small-ill-conditioned-text-grad + late systematic magnitude inflation = the campaign's "near-zero-tensor cos fragility" + "bf16-rounding-in-loops" signature: the joint-attn text grad is small (heavy cancellation) so Mojo-bf16 vs OT-bf16 rounding DECORRELATES and compounds over 24 blocks; the late systematic inflation (and 1.5-2× d_B) is a small op-order/precision difference amplified by conditioning, NOT a discrete wrong-formula bug (sdpa/rope verified exact).
**PRECISION HYPOTHESIS REFUTED (RUN 2026-06-08 — Mojo F32 fix experiment).** Added an env-gated (`KLEIN_F32_BWD`) full-F32 single-block backward path (`klein_stack_lora.mojo` `_sbw_f32`/`_sbsaved_f32`/`_smv_f32` build F32 struct copies; LoRA skipped since B=0; needs BWD scratch slab bumped to 1GB). Dumped per-block running d_x in F32 vs bf16 vs OT:
| | blk23 | blk7 | blk3 | blk0 |
|---|---|---|---|---|
| Mojo-F32 vs Mojo-bf16 (txt) | 1.0000 | 0.9998 | 0.9998 | **0.9995** |
| Mojo-F32 vs OT (txt) | 0.997 | 0.962 | 0.691 | 0.317 |
| Mojo-bf16 vs OT (txt) | 0.997 | 0.962 | 0.692 | 0.318 |
**Mojo's own bf16 & F32 agree to cos ≥0.9995 everywhere ⇒ the text grad is PRECISION-ROBUST, NOT ill-conditioned. F32 does NOT move Mojo toward OT (0.317 vs 0.318 identical).** ⇒ the Mojo↔OT divergence is a REAL DETERMINISTIC ALGORITHMIC DIFFERENCE in Klein's single-block backward, NOT bf16 accumulation. The F32-compute remedy is the WRONG fix here. (.dxall_f32 641MB = 2× bf16 confirms F32 genuinely ran.)
**REVISED MODEL:** a SMALL deterministic formula/constant difference in the single-block backward — present in image too (boundary img cos 0.993≠1.0) but relatively catastrophic for the ~10× smaller text grad, compounding over 24 blocks. Forward loss is EXACT and text participates in attention affecting the image-only output, so it is NOT a text rope-position error (that would perturb the loss). Per-op cos at block 23 was ~1.0 but that only rules out LARGE errors — a small deterministic delta hides under cos~1.0 on big tensors.
**DELTA-BISECTION RUN (2026-06-08) — every BACKWARD op is CORRECT; the root is a FORWARD activation divergence.** Method: INJECT OT's correct grad-at-block-output into Mojo's single block (`KLEIN_INJECT`/`KLEIN_INJECT_BLK` in `klein_stack_lora.mojo`) so ONE block's backward runs on matched input, vs OT. At block 5 (matched input):
- single block adds **10% txt error** (img 0.27%) — the isolated per-block delta.
- segment txt rel-err: d_att 0.9% → sb.d_v 3.5% → d_norm 11.3%. Per-projection: **d_q_pre 19%, d_k_pre 11%, d_v 3.5%** (Q/K worse — the rope+rms path).
- **torch-verified each op IN ISOLATION on Mojo's own dumped inputs**: `rms_norm_backward_dx` 0.16% ✓, `rope_backward` (replicated arithmetic) 0.1% ✓, `sdpa_backward` (torch SDPA autograd) 0.16% ✓. ALL BACKWARD OPS FAITHFUL TO THEIR INPUTS.
- d_att error does NOT amplify: feeding OT-d_att vs Mojo-d_att through the same attention moves d_q only 0.8%.
- ⇒ the divergence is in the INPUTS to the backward = the **SAVED FORWARD activations**. Compared Mojo's saved `q_pre/k_pre/v` (to_qkv_mlp_proj forward output) vs OT's at block 5: **q_pre txt 2.3%, k_pre 1.6%, v 2.9% OFF** (img ~0.6-1.4%). The FORWARD diverges in the text region.
**CONCLUSION:** Klein's single-block BACKWARD MATH IS CORRECT. The d_B/grad parity failure is rooted in a **forward (text-region) activation divergence** vs OT — invisible to the image-only loss (text *query* output is discarded; forward loss exact). The small forward delta (~2-3% txt) maps to a large backward d_q delta (~19%) — partly attention sensitivity on the small txt grad, compounding over 24 blocks.
**FORWARD-DIVERGENCE ROUND (2026-06-08) — partial; an UNEXPLAINED GAP remains (do not assume conditioning).**
- Per-block FORWARD INPUT (Mojo `block_saved.x` vs OT `sfx_i`, `klein_stack_lora.mojo` .dxall `xfwd_`): forward text is ALREADY ~0.5% off at block 0 (single-stack input = double→single boundary), cos ~1.0 (bf16-level), growing slowly to 0.84% at block 23. img 0.6%→1.4%. So the forward divergence enters from the DOUBLES, is SMALL, and grows slowly.
- Within a block the small input (0.5%) maps to q_pre 2.3% (fwd) and d_q_pre 19% (bwd) — a ~38× total amplification.
- BUT a conditioning test REFUTES strong amplification: perturbing Mojo q_rope/k_rope/v by 0.5% moves d_q only ~0.7% (txt); d_att 0.9%→0.8%. So a ~2.3% forward-activation difference should give only ~1-3% d_q change, NOT 19%. **~16% of the d_q divergence is UNEXPLAINED by the measured input differences + attention sensitivity.**
**RESOLVED — the gap was an under-powered perturbation sub-test (sdpa-only, not the full block into the small txt grad). The decisive measurement (forward-vs-grad, txt-vs-img):**
| blk | fwd_txt | fwd_img | grad_txt | grad_img |
|---|---|---|---|---|
| 0 | 0.5% | 0.6% | **142%** | 12% |
| 5 | 0.5% | 0.6% | **50%** | 3% |
| 23 | 0.8% | 1.2% | 8% | 1.6% |
The FORWARD divergence is UNIFORM txt≈img (~0.5-1%, bf16-level, img even slightly higher) — NOT text-specific. The GRADIENT error is ~10× larger for txt than img and GROWS as backward deepens (23→0: 8%→142%). 

**bf16-CONDITIONING HYPOTHESIS REFUTED (2026-06-08, via flame-core PyTorch fixtures `EriDiffusion/flame-core/scripts/generate_klein_*.py`). There IS a real difference, NOT bf16.**
- Built the production structure in PyTorch (joint txt/img attention, IMAGE-ONLY loss so txt grad is small, 12-block stack) and ran bf16 vs F32: **txt-qkv d_B cos 1.0000, ratio ~1.0 at every block** — the text d_B is PRECISION-ROBUST, NOT ill-conditioned. So bf16 forward noise does NOT amplify into a large txt d_B divergence. (Single-stream toy block d_B also bf16-robust: ratio 0.9998.)
- ⇒ Mojo's txt-grad cos-0.32 / d_B 1.5-2× divergence from OT is a REAL deterministic difference, NOT bf16 conditioning. **My earlier "not a bug / bf16-conditioning" conclusion was WRONG — retracted.**
- Verified Mojo FORWARD rms+rope vs the flame-core formula (`torch-rope(head_rms_norm(q_pre,q_norm))` vs Mojo saved q_rope): rel 0.17%, cos 1.0 ✓ — forward rms+rope is correct too.
**FINAL (2026-06-08, VALID F32 reference): NO DISCRETE BUG — it is bf16 op-order amplified by the ill-conditioned text grad.** The per-block double-forward F32 recompute was initially BROKEN (`deepcopy(blk).float()` kept the LoRA monkeypatch's bound bf16 orig_forward → F32==bf16 byte-identical → falsely implied OT-bf16 is F32-exact). FIXED (in-place `blk.float()` after backward). Valid result at double-block-0 (identical input):
| | OT-bf16 vs OT-F32 | Mojo vs OT-bf16 | Mojo vs OT-F32 |
|---|---|---|---|
| txt | 0.30% | 0.27% | 0.28% |
| img | 0.29% | 0.42% | 0.38% |
**OT's OWN bf16 is ~0.3% off its F32 (the reference is NOT F32-accurate), and Mojo-bf16 is NO further from F32 than OT-bf16 is (txt: Mojo 0.28% vs OT 0.30% — Mojo slightly CLOSER). So Mojo's forward is as accurate as OT's; the ~0.3%/block Mojo-vs-OT gap is bf16 OP-ORDER noise between two valid bf16 implementations, NOT a bug.** This amplifies through the (correct) attention + the small ill-conditioned joint-attn TEXT grad into d_B 1.5-2×. RETRACTED: the intermediate "BUG FOUND: Mojo-bf16 vs OT-autocast-F32 norm chain" (artifact of the broken recompute) and the F32-norm "fix" (reverted — didn't help, wrong diagnosis). The earlier flame-core "bf16-conditioning REFUTED" was ALSO based on a not-faithful comparison; the original bf16-op-order-amplified-by-conditioning model is correct.
**SO FOR THE CAMPAIGN:** Klein backward is CORRECT; forward is as-accurate-as-OT bf16. Matching OT-bf16's text d_B exactly is impossible (two valid bf16 impls differ ~0.3%, amplified on the tiny txt grad). The image-relevant grads ARE accurate. REAL OPTIONS: (a) ACCEPT for training — verify Klein LoRA converges (the actual goal); (b) re-baseline the gate to weight by grad magnitude / drop near-zero txt rows; (c) F32 forward+ref for exact parity (full-model F32 OOMs; per-block confirms it's just bf16). DO NOT keep hunting a discrete forward bug — there isn't one (now measured with a valid F32 reference).

**(historical, superseded) GAP CLOSED + comparison validated.** (a) Band/row-order CONFIRMED correct: Mojo d_q_pre matches OT to_qkv-output q-band cos 0.982 (k/v bands cos~0), fwd q_pre cos 0.9997. (b) The 2.3% fwd diff DOES explain the 19% grad: fed Mojo fwd q/k/v vs OT-reconstructed fwd q/k/v (OT `sgl_qkv_fwd` bands → flame-core rms+rope) through the SAME torch attention backward + SAME d_att → d_q_pre txt rel 0.191 cos 0.982 (EXACT match to measured), img 2.6%. The earlier perturbation test under-counted because the real fwd diff is STRUCTURED (shifts the softmax scores), not random bf16.
**CLOSED CHAIN (measured): real STRUCTURED DETERMINISTIC forward difference → amplified by attention.** block-input ~0.5% (uniform txt≈img, from DOUBLES) → single-block forward amplifies TXT specifically 0.5%→2.3% q_pre (img stays 0.6%) → structured 2.3% q/k/v diff → (correct) attention backward amplifies → 19% txt d_q_pre → d_B 1.5-2×. DETERMINISTIC not bf16 (PyTorch joint-attn bf16-vs-F32 txt d_B cos 1.0). The txt amplification is NOT a small-signal/layer_norm effect: txt activations are LARGER than img (block-in std txt 67.7 vs img 26.9; q_pre similar) — so it is a REAL TEXT-SPECIFIC forward difference.
**REMAINING TO LOCALIZE: which forward op produces the structured ~0.5% txt difference at the single-stack input.** It enters from the DOUBLES + the single-block forward amplifies it txt-specifically (modulate/qkv-proj). Suspect TXT-STREAM handling: per-stream modulation (img_mod vs txt_mod) in the single/double block forward, or the txt rows in the double→single concat, or the txt qkv projection. NOT a magnitude/conditioning artifact.
**NEXT (localize the forward op):** (1) verify the Mojo SINGLE-block forward modulate+qkv-proj on a MATCHED OT block-input (forward-inject OT's block-5 input → does Mojo produce OT's q_pre? if yes, root is the doubles; if no, single-block fwd bug). (2) failing that, bisect the DOUBLE-block forward vs OT forward hooks (per-double-block output) to find where the structured 0.5% txt diff is born. (3) OR — cleanest — run Mojo serenitymojo Klein double/single-block FORWARD against flame-core fixtures (`EriDiffusion/flame-core/tests/pytorch_fixtures/patterns/klein_*` + `scripts/generate_klein_*.py`; toy shape, no OOM) to catch the forward op-level diff directly.
**NEXT (use flame-core fixtures — they're the right tool):** the cleanest decisive test is to run the Mojo (serenitymojo) Klein double/single-block FORWARD directly against the flame-core PyTorch fixtures (`tests/pytorch_fixtures/patterns/klein_*.safetensors` — input+expected output at toy shape, F32-derivable by re-running `generate_klein_*.py` with dtype=float32). serenitymojo HAD `double_block_lora_parity`/`klein_stack_lora_parity` gates that consumed these (bit-rotted: SIGILL / StreamLoraGrads drift — REVIVE or rewrite). That gives a clean Mojo-forward-vs-PyTorch comparison at fixture shape with NO model load / NO OOM, and will localize the real forward difference. FIRST: sanity-check the OT-band/row-order assumption in the existing block-5 comparison (cheap, rules out artifact (a)).
**IMPLICATIONS / NEXT (decision, not debugging):**
- The image-relevant grads (the bulk that drives image-generation training) are accurate (≤12%, mostly <3%). The text-query grads are small + noisy (true in OT too; text output is discarded).
- Matching OT-bf16 text d_B exactly is NOT achievable in bf16 — the gate comparing Mojo-bf16 d_B to OT-bf16 d_B will always show the txt components diverging. The gate tolerance (GRAD_REL_EPS 0.05 on TOTAL L2) is dominated by these small txt components.
- OPTIONS: (a) ACCEPT for training — verify Klein LoRA actually trains/converges (loss-down, sample improves), the real goal; the txt-grad noise is small in absolute terms. (b) If exact OT parity is mandated, compute the FORWARD in F32 (reduces the bf16 forward noise feeding the ill-conditioned txt grad) — a Mojo F32-forward path, larger lift. (c) Re-baseline the gate to weight by grad magnitude / exclude the near-zero txt rows.
- Likely re-classify Klein from "❌ BWD BUG" to "✅ backward-correct; txt-grad bf16-conditioning (benign for training)" pending the train-convergence check.
**(superseded) earlier pivots:** F32-fix; forward-text-divergence hunt; close-the-gap. Capture OT forward hidden-states / q_pre/k_pre/v per single block (forward hooks, NOT grads) and diff vs Mojo's saved, walking block 0→23 to find the first divergence and whether it's born in a block or accumulates. Suspects: text-token handling in the single-block forward (modulation/norm/rope for the discarded-output text rows), or the double→single concat. (DEAD ENDS, do not revisit: dtype/bf16; LoRA d_B math; saved/recompute lifetime; sdpa_backward / rope_backward / rms_norm_backward FORMULAS — all torch-verified correct; F32 compute; the single-block backward in general.) Instrumentation all in tree (env-gated; `scripts/ot_klein_block0_dy_oracle.py` SGL_PROBE=5 captures qkv fwd-value + in/out grads; `single_block.mojo` dump_path emits d_att/sb_d_v/d_q_pre/d_k_pre/d_q_rms/sb_d_q/q_rope/q_pre/q_norm/rope tables; `klein_stack_lora.mojo` KLEIN_INJECT + .dxall).
**(obsolete) earlier F32-fix plan:** compute Klein's SINGLE-block (and likely double-block) attention backward in **F32** (upcast q_rope/k_rope/v + d_att into sdpa_backward, rope_backward, the qkv linear backward; cast back at the boundary) — mirrors the campaign's NAVA/chroma "F32 DiT compute" fixes ([[project_nava_first_video_target]], [[project_bf16_carrier_fix]]). Re-run the per-block d_x dump; success = txt cos recovers to ≥0.99 across all 24 blocks → d_B L2 → OT (to_q ~0.000276), gate "differs beyond tolerance" clears. If F32 does NOT fix it → there IS a discrete systematic bug; bisect the single-block d_x assembly op-by-op (modulate_backward / layer_norm_backward / w1 split backward / qkv LoRA d_x fold) for a text-position term. To rule precision in/out decisively first: run an OT-side single-block-0 backward in F32 vs bf16 on the SAME captured inputs (small, fits) and see if OT-F32-vs-OT-bf16 txt cos ALSO drops ~0.3 (=precision-inherent) or stays ~1.0 (=Mojo has a real bug). Instrumentation all in tree (`klein_stack_lora.mojo` KLEIN_DY_DUMP → .dio/.bnd/.sblk/.sdpa/.dxall; `single_block.mojo`+`double_block.mojo` dump_path; `scripts/ot_klein_block0_dy_oracle.py` full hook set).

#### (superseded) original plan — DECISIVE TEST defined, not run
Three agents (~870K tokens) isolated but did NOT fix. State: forward+d_A+residual-d_x all CORRECT (residual d_x matches OneTrainer cos 0.985-0.9999 per block, saved `parity/ot_klein_block_dx.safetensors`). But per-projection LoRA grads (to_q/k/v/out/ff) are 1.5-2× / cos 0.3-0.7 off. Since A(adapter_before)+x(fwd) are identical Mojo-vs-OT, the gap is purely in the **per-projection d_y (d_q/d_k/d_v at projection outputs)** → likely a REAL adjoint error in the joint-attention backward (1.72× too big for bf16). **DECISIVE TEST (do this FIRST, ~1 cycle):** extend `scripts/ot_klein_oracle.py` with `register_full_backward_hook` on double-block-0 `attn.to_q/to_k/to_v` MODULE OUTPUTS (the d_y the LoRA uses — NOT the residual d_x) → dump. Instrument Mojo `src/serenity_trainer/model/klein/double_block.mojo` `double_block_lora_backward_device_resident_scratch` (~:2214) to dump its internal d_q/d_k/d_v. Compare. ALSO compare OneTrainer-bf16-d_y vs OneTrainer-F32-d_y (one block) to rule out dtype. If a real adjoint bug → it points at the exact op (suspect git commit `5839693 Split Klein qkv row outputs`). Fix in serenitymojo `models/klein/{double_block,lora_block}.mojo` AND the port copy. GATE: `smoke/klein_train_ref_grad_update_replay.mojo` d_B L2 → OneTrainer (to_q ~0.000265), "differs beyond tolerance" clears, forward+d_A preserved. NOTE: serenitymojo klein torch gates are BIT-ROTTED (`klein_stack_lora_parity` SIGILL; `double_block_lora_parity` won't build — StreamLoraGrads API drift) — use the OneTrainer oracle, not them.

### B. Qwen oracle + gate
Build `scripts/ot_qwen_oracle.py` templated on ot_chroma/klein. OneTrainer `QwenModel`+`BaseQwenSetup.predict`+`QwenLoRASetup`. Frozen `parity/qwen_train_ref_step000.safetensors` (latent 2,16,1,64,64; text_encoder_hidden_state 2,512,3584 — CHECK if real or placeholder zeros). Adapters 2160 (720×3, 60-block attn+mlp). Ckpt: find in `serenitymojo/training/train_qwenimage_real.mojo`. **~20B → OOM risk on 24GB; report if can't fit (try offload/single-sample).** Then gate Mojo qwen vs ot_qwen_grads.

### C. SD 3.5 — biggest lift
No serenitymojo torch gate, prior audit flagged missing TE/cache. Build OneTrainer oracle from `StableDiffusion3Model`+`BaseStableDiffusion3Setup.predict` (MMDiT, dual CLIP + T5, flow-match). Frozen `parity/sd3_train_ref_step000.safetensors` (partial — check `sd3_train_ref_blockers.json`: `data_loader_registered:false`, `text_encoder_key_count:0`). serenitymojo has `models/sd35/sd35_stack_lora.mojo` (:706 backward) + `train_sd35_real.mojo`. May need: build the port wiring (import-style like chroma/ernie), a forward-replay smoke, and the OneTrainer oracle. Likely needs more than 1 agent cycle.

### D. Carried-over (lower priority)
- **SDXL Stage 2**: conv/embed/proj LoRA math is torch-gated (proj 48/48, conv 13/13, embed 12/12) but NOT wired into the real UNet loop. Reference now exists: `ot_sdxl_grads.safetensors` (full-UNet, total grad-L2 0.010614). Thread conv LoCon (`training/locon_conv_adapter.mojo`) + embed/proj linear LoRA through `sdxl_real_train.mojo` resblock fwd/bwd + populate `build_sdxl_lora_set`.
- **Direct Mojo-vs-OneTrainer grad re-gates** for Chroma/Ernie/Anima: oracles + ot_*_grads exist; just run the Mojo grad smoke and diff per-LoRA grads vs ot_*_grads (the dumps were post-AdamW / version-skewed; OneTrainer grads are cleaner).

## 4. METHOD / GOTCHAS (binding)
- MAIN LOOP owns every gate — re-run every agent's number on a clean serial build before counting it (Tenet 4). Agents do NOT build concurrently.
- Per-model verify order: (1) serenitymojo torch lora_stack_parity [fast math check] (2) frozen forward replay [catches arch simplifications the torch gate MISSES — e.g. Anima rope] (3) OneTrainer oracle [authoritative]. The torch gate gates rope-APPLICATION not rope-POSITIONS — a shared source simplification is invisible to it.
- d_A=0 invariant: B zero-init ⇒ raw d_A==0 exactly; noise-immune structural backward check.
- The frozen dumps are mostly OneTrainer-faithful (Klein/Chroma/SDXL exact; Ernie/Anima small fork residuals). Don't assume they're bit-exact; OneTrainer oracle is authoritative.
- bf16-rounding-in-loops, dtype bad-reference trap, near-zero-tensor cos fragility — all seen this campaign; see numeric-parity-testing skill.
- Import-style port (thin wrapper over serenitymojo, e.g. Chroma) trains-with-parity but isn't self-contained — tech debt vs the Klein-style copy-in. Acceptable for the goal; flagged.

## 4b. GIT STATE — ALL UNCOMMITTED (both repos), verified present
- **serenity-trainer** (untracked): `docs/{HANDOFF_PARITY_2026-06-08,TRAINING_PARITY_CAMPAIGN,TRAINING_READINESS_AUDIT,SERENITYMOJO_REUSE_REPORT}.md`, `scripts/ot_*_oracle.py` (6), `parity/ot_*_grads.safetensors` (6), `smoke/{anima,chroma,ernie,sdxl}_train_ref_{forward,grad_update}_replay.mojo`, `src/serenity_trainer/model/{anima,chroma,ernie,sdxl}/`. Modified: `src/serenity_trainer/modelSetup/Flux2LoRASetup.mojo`, some `ui/*`.
- **mojodiffusion** (modified, verified in tree): `serenitymojo/models/sdxl/{sdxl_real_train.mojo (sdxl_real_forward[H,W] :289),sdxl_unet_stack_lora.mojo,parity/*}`, `serenitymojo/models/ernie/{block.mojo (gelu_exact :299),lora_block.mojo}`, `serenitymojo/ops/{norm.mojo,norm_backward.mojo,activation_backward.mojo (gelu_exact_backward)}`, `serenitymojo/training/{train_anima_real.mojo (real 3D rope :443),train_sdxl_real.mojo}`. (Other modified files predate this session: ideogram4*, ldm_encoder, nava*.)
- Nothing committed this session. Klein agents reverted their instrumentation (tree clean for klein). The Anima/Ernie/SDXL fixes PERSISTED (verified).

## 5. KEY FILES
- Campaign log: `docs/TRAINING_PARITY_CAMPAIGN.md`. Audits: `docs/TRAINING_READINESS_AUDIT.md`, `docs/SERENITYMOJO_REUSE_REPORT.md`.
- Oracles: `scripts/ot_*_oracle.py`. Grad refs: `parity/ot_*_grads.safetensors`.
- Port verticals: `src/serenity_trainer/model/{chroma,ernie,anima,sdxl,klein}/`. Smokes: `smoke/<m>_train_ref_{forward,grad_update,loss}_replay.mojo`.
- serenitymojo source: `/home/alex/mojodiffusion/serenitymojo/models/<m>/`, torch gates `.../parity/lora_stack_parity.mojo`.
- Memory: `project_serenity_trainer_parity_campaign`. STALE memories to clean (point at deleted dirs): `project_onetrainer_mojo_ideogram4`, `project_anima_fork_ports`.
